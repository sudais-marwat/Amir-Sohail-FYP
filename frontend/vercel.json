{
  "buildCommand": "cd .. && npm install --no-audit --no-fund && npm run build --workspace frontend",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
